/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    output: 'export',
    basePath: '/trueHack-hookah',
    images: {
        unoptimized: true,
    }
};

module.exports = nextConfig;
